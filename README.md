# weatherAppPython

Experimental Python Weather and Airlines implementation with SQL database features to view weather and airline data with api pulls. 

## Run

Use the project virtual environment so Pillow and the map renderer are available:

```powershell
.\.venv\Scripts\python.exe weather.py
```
